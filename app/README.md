# app/

The Expo / React Native app (iOS + Android). Not yet generated — create with
`npx create-expo-app` at build start. Owns: coaching chat (voice + text),
transcript store (device custody), approval/preview UI, situations, pairing,
staging, resolution, settings (backup, opt-ins, subscription).
